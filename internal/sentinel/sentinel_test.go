package sentinel

// Tests temporarily disabled for POC sentinel-db branch.
// The sentinel now uses queue.Publisher instead of broker.Publisher,
// and tests need to be rewritten to use the new queue-based approach.
